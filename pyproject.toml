[project]
name = "python-playground"
version = "0.1.0"
description = "This project contains the scripts and programs created while I was learning and playing Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.uv.sources]
pdf = { workspace = true }

[tool.uv.workspace]
members = [
    "scripts/*"
]
